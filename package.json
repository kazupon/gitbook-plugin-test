{
  "name": "gitbook-plugin-test",
  "description": "gitbook plugin for test",
  "version": "0.0.1",
  "author": "kazuya kawaguchi",
  "engines": {
    "gitbook": ">2.x.x"
  },
  "gitbook": {
    "properties": {
      "myConfigKey": {
        "type": "string",
        "default": "it's the default value",
        "description": "It defines my awesome config!"
      }
    }
  },
  "license": "MIT",
  "main": "index.js",
  "private": true,
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
